{% set mongodb = salt['grains.filter_by']({
    'Debian': {
        'dbpath': '/data',
        'databases': {},
        'configuration': {
          'databases': {}
        },
        'rs': None,
        'rs_members': [],
        'auth': None,
        'key_string': None,
        'use_native_package': True,
    },
    'default': 'Debian',
}, merge=salt['pillar.get']('mongodb',{})) %}
